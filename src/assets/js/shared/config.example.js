// ============================================
// API Configuration - Cybhor Tech Portal
// ============================================
// Copie este arquivo como config.js e ajuste conforme seu ambiente.
// Diferente da era Firebase, config.js não guarda mais nenhum segredo
// (a API não expõe chaves ao cliente), então pode ficar versionado.

// Em produção (atrás do proxy reverso do docker-compose), deixe vazio:
export const API_BASE_URL = '';

// Em desenvolvimento local, com a API rodando separada do frontend,
// aponte para o endereço completo do backend:
// export const API_BASE_URL = 'http://localhost:4000';
